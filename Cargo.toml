[package]
name = "abow"
version = "0.1.0"
authors = ["Alex Maiorella <alex@maiorella.org>"]
edition = "2018"

[features]
default = ["useopencv"]
useopencv = ["opencv"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
opencv = { version = "0.46", optional = true }
bitvec = "0.20"
rand = "*"
bincode = "*"
thiserror = "*"

[[example]]
name = "match"
required-features = ["useopencv"]

[[example]]
name = "create-voc"
required-features = ["useopencv"]

[profile.release]
debug = true
